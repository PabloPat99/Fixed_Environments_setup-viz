# Fixed_Environments_setup-viz
This project simulates structured environment data by generating hostnames based on predefined proportions for operating systems, environments, and countries. It organizes the generated information into a DataFrame and uses visualizations to analyze the distribution of environments across different countries.
